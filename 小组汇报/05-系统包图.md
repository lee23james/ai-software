# 05 系统包图

## 1. 后端包结构

当前后端主要包结构如下：

```text
backend/src/main/java/com/example/jobplatform
├── common
├── config
├── controller
├── dto
├── entity
├── exception
├── mapper
├── service
├── service/impl
├── vo
└── JobPlatformApplication.java
```

## 2. 后端包图

```mermaid
flowchart TB
    App["JobPlatformApplication"]

    subgraph Backend["com.example.jobplatform"]
        Controller["controller\n接口控制层"]
        Service["service\n业务接口"]
        ServiceImpl["service.impl\n业务实现"]
        Mapper["mapper\n数据访问层"]
        Entity["entity\n数据库实体"]
        DTO["dto\n请求参数对象"]
        VO["vo\n响应展示对象"]
        Common["common\n统一返回结构"]
        Config["config\n系统配置"]
        Exception["exception\n全局异常处理"]
    end

    App --> Controller
    Controller --> Service
    Controller --> DTO
    Controller --> VO
    Controller --> Common
    Service --> ServiceImpl
    ServiceImpl --> Mapper
    ServiceImpl --> VO
    Mapper --> Entity
    Exception --> Common
    Config --> Backend
```

## 3. 前端包结构

当前前端主要目录结构如下：

```text
frontend/src
├── api
├── assets
├── layouts
├── router
├── stores
├── views
├── App.vue
└── main.js
```

## 4. 前端包图

```mermaid
flowchart TB
    Main["main.js"]
    App["App.vue"]

    subgraph Frontend["frontend/src"]
        Router["router\n路由配置"]
        Stores["stores\nPinia 状态管理"]
        API["api\nAxios 接口封装"]
        Layouts["layouts\n系统布局"]
        Views["views\n页面组件"]
        Assets["assets\n样式与静态资源"]
    end

    Main --> App
    Main --> Router
    Main --> Stores
    App --> Router
    Router --> Views
    Views --> Layouts
    Views --> API
    Views --> Stores
    Views --> Assets
```

## 5. AI 服务包结构

当前 AI 服务主要目录结构如下：

```text
ai-service/app
├── routers
├── schemas
├── services
└── main.py
```

## 6. AI 服务包图

```mermaid
flowchart TB
    MainPy["main.py\nFastAPI 应用入口"]

    subgraph AI["ai-service/app"]
        Routers["routers\n接口路由"]
        Schemas["schemas\n请求数据模型"]
        Services["services\nAI 业务逻辑"]
    end

    MainPy --> Routers
    Routers --> Schemas
    Routers --> Services
```

## 7. 汇报回答口径

如果被问到“项目包图是怎样的”，可以回答：

> 后端按照 Spring Boot 常见三层架构划分为 controller、service、service.impl、mapper、entity、dto、vo、common、config、exception 等包；前端按照 Vue 项目结构划分为 views、layouts、router、stores、api、assets；AI 服务按照 FastAPI 项目结构划分为 routers、schemas、services。这样的包划分能够清晰区分页面展示、接口请求、业务处理、数据访问和 AI 计算，方便团队成员并行开发。

